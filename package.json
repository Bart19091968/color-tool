{
  "name": "color-analyzer",
  "version": "2.0.0",
  "description": "Color Analyzer — Find color names, HEX, RGB, HSL and palettes",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "dependencies": { "express": "^4.18.2" },
  "engines": { "node": ">=18.0.0" }
}
